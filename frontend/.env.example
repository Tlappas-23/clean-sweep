# Copy to `.env.local` to override. Both are optional.

# Run the whole UI against the in-memory mock adapter (no backend needed).
VITE_API_MOCK=false

# Absolute base URL for the API. Leave empty to use the same origin (the Vite
# dev server proxies /api and /health to http://localhost:8000).
VITE_API_BASE=
